import { z } from 'zod';

type ToolParametersStatic<T extends z.ZodSchema> = {
    new (): z.infer<T>;
    schema: T;
};
declare function createToolParameters<T extends z.ZodSchema>(schema: T): ToolParametersStatic<T>;

export { type ToolParametersStatic, createToolParameters };
